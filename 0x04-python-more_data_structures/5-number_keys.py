#!/usr/bin/python3
def number_keys(a_dictionary):
    return len(a_dictionary.keys())


"""
    no_keys = 0
    for key, a in enumerate(a_dictionary):
        no_keys = no_keys + key
    return no_keys
"""
